/**
 * Function to return dummy projects data.
 * 
 * @returns {Array} Array of project objects with id and title properties.
 */
const projectsData = () => [
    {
        id: 1,
        title: "React",
    }
]

export default projectsData;